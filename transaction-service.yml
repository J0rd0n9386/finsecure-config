server:
  port: 8082

msg: "FinSecure Transaction Service - Deposit & Withdraw"

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: health,info,refresh